import { component, html, css } from "https://cdn.jsdelivr.net/npm/wolf-framework@1.0.1";

// Import any components you need via `import "path/to/component.js"`


component("home", () => {
    return html`
        <h1>Hallo Welt!</h1>
    `
});